name: CI/CD

on:
  push:
    branches: [ "main" ]
  # pull_request:
  #   branches: [ "main" ]

jobs:
  # build-test:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Set up JDK 21
  #     uses: actions/setup-java@v4
  #     with:
  #         java-version: '21'
  #         distribution: 'temurin'
          
  #   - name: Build and Test Backend
  #     working-directory: ./
  #     run: |
  #         ./mvnw clean install
  #         ./mvnw test
  build-push-docker:
    runs-on: ubuntu-latest
    # needs: build-test

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag krystine825/expense_tracker:latest
  
  deploy:
    runs-on: ubuntu-latest
    needs: build-push-docker

    steps:
    - name: Trigger ArgoCD Sync
      run: |
        argocd login 35.240.211.197 --username admin --password admin
        argocd app sync expense-tracker
